# Frontend Environment Variables
# Leave empty for local development (uses Vite dev server proxy to localhost:8000)
# Set to full backend URL if frontend is deployed separately on Vercel/Netlify:
# VITE_API_URL=https://your-flux-api.onrender.com
VITE_API_URL=
